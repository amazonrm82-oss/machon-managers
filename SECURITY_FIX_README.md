# תיקון אבטחה קריטי — סיסמאות ונתונים בגלוי בקוד הלקוח

מסמך זה כולל שני שלבים שהתגלו וטופלו ברצף באותו PR: **שלב 1** — סיסמאות בגלוי,
**שלב 2** — כל נתוני `app_state` (תקציבים/משכורות/עובדים) פתוחים לכולם. שלב 3
(עתידי, לא בוצע) מתועד בסוף המסמך. יש לבצע את **כל** שלבי ה-Supabase (1–2) לפני
מיזוג ה-PR — פירוט מדויק בהמשך.

## מה היה הבעיה (שלב 1 — סיסמאות)

עד לתיקון הזה, כל הסיסמאות של המערכת (מנהל תפעול ארצי, נשיא, מנהלת תקציבים, אב בית,
מנהל/צוות של כל מכון, וקוד השחזור של המנהל הארצי) היו נגישות לכל מי שנכנס לאתר
הציבורי, **בלי להתחבר בכלל ובלי לפצח שום דבר**:

1. ערכי ברירת המחדל היו מוטמעים כטקסט גלוי ב-JavaScript של `index.html` עצמו
   (`const INSTITUTE_PASSWORDS = {...}` וכו').
2. הסיסמאות **הנוכחיות בפועל** (גם אם שונו על ידי מנהל) נשמרו כחלק מ-blob ה-state
   הכללי בטבלת `app_state` ב-Supabase, ונשלפות במלואן בכל טעינת עמוד דרך ה-anon key
   הציבורי — כלומר גם קריאת HTTP ישירה לשרת (`GET .../rest/v1/app_state?id=eq.1`)
   חושפת את כל הסיסמאות, בלי שום בדיקת הרשאה.

## מה תוקן בקוד (כבר בוצע, חלק מה-PR הזה)

- הוסרו כל קבועי הסיסמאות המוטמעים מ-`index.html`.
- `rolePasswords` הוסר לגמרי ממנגנון הסנכרון הכללי (`PERSIST_FIELDS`) — הוא כבר לא
  נשלח ל-Supabase, לא נשמר ב-localStorage, ולא נכלל בקובצי גיבוי.
- כל בדיקת סיסמה (התחברות, שינוי סיסמה עצמי, עריכת סיסמה ע"י מנהל ארצי, יצירת
  מכון חדש, שחזור סיסמת מנהל ארצי) עוברת עכשיו דרך קריאת רשת ל-Edge Function חדשה
  בשם `role-auth`, שהיא היחידה שמורשית לקרוא/לכתוב סיסמאות אמיתיות בצד השרת.
- אותו תיקון בוצע ב-`projects.html`, שסבל מאותה בעיה בדיוק (שולף `rolePasswords`
  מאותה טבלת `app_state`).
- מסך "מצב מערכת" עדיין מאפשר למנהל הארצי **לקבוע** סיסמה חדשה לכל תפקיד, אבל כבר
  לא **מציג** את הסיסמה הקיימת (השדה מוצג כ"מוסתר מטעמי אבטחה") — זו בדיוק ההרשאה
  שהייתה החור: הצגת סיסמאות אמיתיות בדפדפן.

**שינו קוד בלבד — לא שונה אף סיסמה קיימת בפועל, ולא נמחקו נתונים.**

## מה עדיין צריך לבצע בפועל ב-Supabase (לא בוצע ולא ניתן לביצוע מכאן)

הסביבה שבה בוצע התיקון חסומה מגישת רשת ל-Supabase, ולכן הצעדים הבאים **חייבים**
להתבצע ידנית ע"י מישהו עם גישה ללוח הבקרה של הפרויקט
(`https://supabase.com/dashboard/project/tnfjcmmblrkhypprjacq`), **לפני** שממזגים
את ה-PR הזה ל-main (אחרת האתר החי יישבר — אף אחד לא יוכל להתחבר):

### 1. להריץ את המיגרציה (יוצרת את הטבלאות)

ב-SQL Editor של Supabase, להריץ את התוכן של
`supabase/migrations/20260908_role_passwords.sql`.

### 2. לזרוע (seed) את הסיסמאות האמיתיות הנוכחיות

**לפני** שמפעילים את הקוד החדש, יש להיכנס לאפליקציה הישנה (עוד לפני שה-PR עולה),
להיכנס כ-`national`, לפתוח את מסך "מצב מערכת", ולרשום את כל הסיסמאות/מזהי המכונים
הקיימים בפועל כרגע (הם עלולים להיות שונים מברירות המחדל שבתיעוד המקורי, אם מישהו
כבר שינה אותם). לאחר מכן להריץ ב-SQL Editor (**לא** לשמור קובץ הזה עם ערכים אמיתיים
בשום מקום ב-git):

```sql
insert into role_passwords (role_key, password) values
  ('national', '<הסיסמה הנוכחית>'),
  ('president', '<...>'),
  ('budgetManager', '<...>'),
  ('buildingManager', '<...>'),
  ('nationalRecoveryCode', '<...>'),
  ('institute_<מזהה מכון 1>', '<...>'),
  ('staff_<מזהה מכון 1>', '<...>')
  -- ... שורה זוגית (institute_/staff_) לכל מכון קיים בפועל
on conflict (role_key) do update set password = excluded.password, updated_at = now();
```

### 3. לפרוס את ה-Edge Function

באמצעות ה-Supabase CLI (מותקן ומחובר לפרויקט — כפי שכבר נעשה בעבר עבור
`send-notification`):

```bash
supabase functions deploy role-auth --project-ref tnfjcmmblrkhypprjacq
```

הפונקציה משתמשת אוטומטית במשתני הסביבה `SUPABASE_URL` ו-`SUPABASE_SERVICE_ROLE_KEY`
ש-Supabase מזריק לכל Edge Function כברירת מחדל — אין צורך להגדיר Secrets נוספים.

### 4. `app_state` — ראו שלב 2 למטה

בבדיקה בפועל (Authentication → Policies) התברר ש-`app_state` (התקציבים, המשכורות,
העובדים של **כל** המכונים) הייתה פתוחה לקריאה **וכתיבה** לכל אחד באינטרנט, בלי שום
התחברות — בעיה נפרדת וחמורה לא פחות מהסיסמאות. שלב 2 למטה סוגר אותה.

### 5. בדיקה (מומלץ, אחרי שהצעדים 1–3 בוצעו, ולפני מיזוג ה-PR)

לוודא שהאתר עדיין רץ עם `index.html`/`projects.html` הישנים (עוד לפני המיזוג) —
הם ימשיכו לעבוד גם עם הטבלאות/הפונקציה החדשות קיימות, כי הם לא קוראים להן. רק
אחרי שממזגים את ה-PR, האתר עובר בפועל לבדיקת הסיסמאות מול ה-Edge Function — אז זה
הרגע לבדוק בדפדפן אמיתי שהתחברות עם כל התפקידים עדיין עובדת.

## שלב 2 — נעילת `app_state` (תקציבים, משכורות, עובדים של כל המכונים)

### מה התגלה

בבדיקה בפועל דרך Authentication → Policies התברר ש-`app_state` — הטבלה עם **כל**
הנתונים התפעוליים של **כל** המכונים (תקציבים, משכורות, פרטי עובדים, קופה קטנה
וכו') — הייתה פתוחה לקריאה (`SELECT`) **וגם לכתיבה** (`UPDATE`) לכל אחד באינטרנט,
בלי להתחבר בכלל. חמור מכך: האפליקציה שלפה את **כל** הבלוב הזה מיד עם טעינת הדף,
**לפני** שמישהו בכלל מזין סיסמה — מסך ההתחברות היה רק "וילון" ויזואלי, הנתונים כבר
ישבו בזיכרון הדפדפן.

### מה תוקן בקוד (כבר בוצע, חלק מאותו PR)

- נוסף Edge Function חדש — `app-state-gateway` — שהוא כעת הדרך היחידה שבה
  `index.html`/`projects.html` קוראים/כותבים את `app_state`.
- לפני התחברות: נשלפת **רק** רשימת שמות/מזהי מכונים (לתפריט הבחירה במסך הכניסה) —
  לא שום נתון רגיש.
- אחרי התחברות מוצלחת: `role-auth` (מעודכן) מנפיק גם "טוקן" חתום לכל היותר ל-24
  שעות, שרק איתו `app-state-gateway` מסכים לקרוא/לכתוב את הנתונים המלאים.
- **חשוב**: זה עדיין לא הפרדה מלאה בין מכונים (מנהל מכון X עדיין יכול טכנית לגשת
  לנתוני מכון Y ברגע שהוא מחובר) — זה שלב הבא, גדול יותר, שידובר בנפרד. השלב הזה
  סוגר רק את החור הכי חמור: גישה בלי להתחבר בכלל.

### מה צריך להריץ ב-Supabase — **סדר קפדני, כדי שלא תהיה נפילה של האתר החי באמצע**

⚠️ שימו לב לסדר: מוקדם יותר במסמך הזה הצעדים היו במספור אחר ((א) drop policy קודם) —
זה **היה שגוי** ותוקן כאן. אם מורידים את מדיניות ה-`anon` הישנה על `app_state` **לפני**
שהקוד החדש עולה ל-main, האתר החי (שעדיין רץ עם הקוד הישן, שקורא ל-`app_state` ישירות
עם ה-anon key) נשבר באופן מיידי לכולם. הסדר הנכון:

**א. להגדיר סוד משותף לחתימת הטוקנים** — ב-Edge Functions → Manage secrets (או
Project Settings → Edge Functions → Secrets), להוסיף secret בשם:

```
AUTH_TOKEN_SECRET
```

עם הערך (מחרוזת אקראית שנוצרה במיוחד לצורך זה — אין צורך לזכור אותה, רק להדביק
פעם אחת):

```
-_qnBPyKDZwsRLi6nAbygetgnb0N1_vANFahtjLh9wy1A4fo0r1-Si99jk3-lzJf
```

**ב. לעדכן מחדש (redeploy) את `role-auth`** — הקוד השתנה (עכשיו גם מנפיק טוקן).
בעורך הפונקציה הקיימת ב-Supabase (Edge Functions → role-auth → עריכה), למחוק את כל
התוכן הקיים ולהדביק את התוכן המעודכן מהקובץ `supabase/functions/role-auth/index.ts`
בריפו (בענף הזה), ולפרוס מחדש.

**ג. לפרוס פונקציה חדשה — `app-state-gateway`** — בדיוק כמו שפרסתם את `role-auth`
בשלב 3 למעלה (Edge Functions → Deploy a new function → Via Editor → שם:
`app-state-gateway`), ולהדביק את התוכן המלא מהקובץ
`supabase/functions/app-state-gateway/index.ts` בריפו.

**ד. למזג את ה-PR ל-main** — רק אחרי א'–ג'. בשלב הזה `app_state` עדיין פתוחה גם
ל-anon כמו קודם (רשת ביטחון), אז אין סיכון נפילה: הקוד הישן (עד למיזוג) ממשיך לעבוד
עם הגישה הישנה, והקוד החדש (אחרי המיזוג) כבר יודע להשתמש בשער החדש.

**ה. לבדוק בדפדפן חי, אחרי המיזוג** — שכל תפקיד מתחבר, שהנתונים נטענים, ושאפשר
לשמור שינוי (למשל לערוך תקציב ולראות שהוא נשמר).

**ו. רק אחרי שה' עבר בהצלחה — לנעול סופית את `app_state`** — ב-SQL Editor:

```sql
drop policy if exists "anon can read app_state" on app_state;
drop policy if exists "anon can update app_state" on app_state;
```

(משאירים RLS **מופעל** על הטבלה בלי אף policy — בדיוק כמו ב-`role_passwords` —
כך שמעכשיו אין יותר שום גישה ישירה דרך ה-REST API; רק ה-Edge Function, עם
ה-service_role key, יכול לגעת בטבלה.) זה הצעד שסוגר את החור בפועל — עד שהוא לא
רץ, `app_state` עדיין פתוחה לכולם (גם אם הקוד החדש כבר לא משתמש בזה).

### ⚠️ למה חשוב לא לדלג על שלב ו'

בלי שלב ו', כל התיקון של שלב 2 הוא בעיקר קוסמטי: הקוד החדש לא ניגש ל-`app_state`
ישירות יותר, אבל מי שיודע לקרוא ל-REST API עם ה-anon key ישירות (בלי לעבור דרך
האתר בכלל) עדיין יכול היום. **חשוב לחזור ולוודא ששלב ו' בוצע בפועל** אחרי שהאתר
נבדק ועובד.

## המלצה עתידית (לא בוצעה כאן, מעבר להיקף התיקון הזה)

הסיסמאות נשמרות כרגע בטבלת `role_passwords` כטקסט רגיל (לא מגובב/hashed) — משופר
משמעותית לעומת המצב הקודם (כי היא לא נגישה בלי ה-service_role key, שלא יוצא
מהשרת), אבל ניתן לשפר עוד: לעבור ל-hash (למשל bcrypt) כדי שגם מי שמקבל גישת קריאה
ישירה למסד הנתונים (למשל דרך Supabase Dashboard, או גיבוי שדלף) לא יראה סיסמה
בפועל. זו הרחבה סבירה להמשך, לא נכללה כאן כדי לשמור את התיקון הזה ממוקד וניתן
לבדיקה.

## שלב 3 — `projects`, `project_tasks`, `project_updates`, `project_documents`, `push_subscriptions` (הקוד בוצע; שלבי Supabase ידניים)

באותה בדיקת Policies התגלה ש-`projects` ו-`project_documents` פתוחות לגמרי
(policy בשם "anon can manage ..." עם COMMAND=`ALL`), וכנ"ל `push_subscriptions`.
בפועל גם `project_tasks` ו-`project_updates` (משימות ועדכונים של כל פרויקט) היו
באותו מצב. כלומר כל הפרויקטים, המשימות, העדכונים, המסמכים המועלים ומנויי ה-Push
של כל המכונים היו ניתנים לקריאה/שינוי/מחיקה לכל אחד באינטרנט עם ה-anon key, בלי
להתחבר בכלל — בדיוק אותו סוג חור שנסגר ל-`app_state` בשלב 2.

### מה תוקן בקוד (כבר בוצע, בענף הזה)

- נוסף Edge Function חדש — `projects-gateway` — שהוא כעת הדרך היחידה שבה
  `projects.html` (וגם `index.html`, עבור `push_subscriptions`) קורא/כותב את חמש
  הטבלאות האלה. הפונקציה דורשת טוקן חתום תקף (אותו טוקן שמנפיק `role-auth` בהתחברות
  מוצלחת, שמאומת גם ב-`app-state-gateway`) לכל פעולה — אין בה שום פעולה ציבורית.
- `projects.html` עבר לקרוא ל-`callProjectsGateway`/`pgRows` בכל מקום שבו קודם פנה
  ישירות ל-`/rest/v1/projects|project_tasks|project_updates|project_documents`.
- `index.html` מפנה עכשיו את שמירת/מחיקת מנוי ה-Push דרך אותה פונקציה.

**היקף (כמו שלב 2):** זה סוגר רק גישה בלי התחברות. הוא עדיין **לא** מפריד בין
מכונים (כל תפקיד מחובר עדיין יכול טכנית לגעת בפרויקטים של כל מכון) — זה השלב הבא.

### מה צריך להריץ ב-Supabase — **סדר קפדני** (אחרת האתר החי נשבר באמצע)

⚠️ בדיוק כמו בשלב 2: אם מורידים את מדיניות ה-anon על הטבלאות **לפני** שהקוד החדש
עלה ל-main והפונקציה נפרסה, האתר החי (הקוד הישן, שקורא ל-`/rest/v1/...` ישירות)
נשבר מיד. הסדר הנכון:

**א. לפרוס את הפונקציה החדשה `projects-gateway`** — Edge Functions → Deploy a new
function → Via Editor → שם: `projects-gateway`, ולהדביק את התוכן המלא מהקובץ
`supabase/functions/projects-gateway/index.ts` בענף הזה. היא משתמשת באותו
`AUTH_TOKEN_SECRET` שכבר הוגדר בשלב 2 (אין secret חדש), וב-`SUPABASE_URL` /
`SUPABASE_SERVICE_ROLE_KEY` שמוזרקים אוטומטית.

**ב. למזג את ה-PR ל-main** — רק אחרי א'. בשלב הזה חמש הטבלאות עדיין פתוחות ל-anon
כרשת ביטחון, אז אין נפילה: הקוד הישן ממשיך לעבוד עד המיזוג, והקוד החדש (אחרי
המיזוג) כבר עובר דרך השער.

**ג. לבדוק בדפדפן חי, אחרי המיזוג** — להתחבר ל-`projects.html` (national ומנהל מכון),
לוודא שרשימת הפרויקטים/המשימות/המסמכים נטענת ושאפשר ליצור/לערוך/למחוק פרויקט
ומשימה, ושהפעלת/כיבוי התראות ב-`index.html` עדיין עובדות.

**ד. רק אחרי שג' עבר — לנעול את חמש הטבלאות** — ב-SQL Editor:

```sql
drop policy if exists "anon can manage projects" on projects;
drop policy if exists "anon can manage project_tasks" on project_tasks;
drop policy if exists "anon can manage project_updates" on project_updates;
drop policy if exists "anon can manage project_documents" on project_documents;
drop policy if exists "anon can manage push_subscriptions" on push_subscriptions;
```

(שמות ה-policy עשויים להיות שונים אצלכם — ב-Authentication → Policies אפשר לראות
את השם המדויק לכל טבלה ולמחוק אותו. משאירים RLS **מופעל** על כל טבלה בלי אף policy,
בדיוק כמו ב-`app_state`/`role_passwords`, כך שרק ה-Edge Function עם ה-service_role
key יכול לגעת בהן.) עד שצעד ד' רץ, הטבלאות עדיין פתוחות — הקוד כבר לא משתמש בגישה
הישירה, אבל מי שקורא ל-REST עם ה-anon key ישירות עדיין יכול, בדיוק כמו שהוסבר על
`app_state` בשלב 2.

> `send-notification` ממשיך לרוץ כרגיל — הוא לא נגע בשינוי הזה. `project_tasks`/
> `project_updates` נכללים ב-`projects-gateway` למרות שלא הופיעו במיפוי המקורי, כי
> `projects.html` פונה גם אליהם ישירות.

בנוסף: השלב הנוכחי (2) מוודא רק שמי שניגש ל-`app_state` **התחבר בפועל** עם סיסמה
נכונה — הוא **לא** מוודא שמנהל מכון אחד לא יכול לגעת בנתוני מכון אחר (כל תפקיד
מחובר עדיין רואה/יכול לשנות את כל הבלוב, כמו שהיה גם לפני התיקון). הפרדה אמיתית
בין מכונים דורשת עוד שכבת בדיקות בצד השרת (מי מורשה לגעת באיזה מכון) — פרויקט
נפרד ומשמעותי, לא בוצע כאן.

## שלב 4 — הפרדה בין מכונים (בתהליך; התחלנו ב-`projects-gateway`)

המטרה: תפקיד מכון (`institute_<id>` / `staff_<id>`) יוכל לגעת רק בנתוני המכון שלו,
בעוד תפקידים ארציים (national / president / budgetManager / buildingManager)
נשארים עם גישה כלל-ארגונית. ההפרדה נבנית משטח-אחר-משטח.

### שלב 4א — פרויקטים (הקוד בוצע; דורש redeploy של הפונקציה)

`projects-gateway` עודכן כך שהוא גוזר את המכון מתוך ה-`roleKey` שבטוקן ואוכף אותו על
כל פעולה: תפקיד מכון יכול לרשום/לקרוא/לערוך/למחוק רק פרויקטים, משימות, עדכונים
ומסמכים ששייכים למכון שלו, ואינו יכול ליצור פרויקט במכון אחר או להעביר פרויקט
למכון אחר. תפקידים ארציים — ללא שינוי. `push_subscriptions` נשאר לפי-מכשיר (לא
מוגבל למכון). אין שינוי בקוד הלקוח — הלקוח ממילא מבקש רק את המכון של המשתמש; השרת
פשוט אוכף זאת גם מול בקשה מזויפת.

**מה צריך לעשות:** לפרוס מחדש (redeploy) את `projects-gateway` עם התוכן המעודכן
מהקובץ `supabase/functions/projects-gateway/index.ts` (Edge Functions →
projects-gateway → עריכה → הדבקת התוכן החדש → Deploy). אין שלב SQL ואין שינוי
policy — האכיפה כולה בקוד הפונקציה. שינוי תואם-לאחור: הקוד הישן והחדש עובדים עם
אותו לקוח, אז אין חלון נפילה.

### שלב 4ב — `app_state` (טרם בוצע)

התקציבים/המשכורות/העובדים חיים ב-blob יחיד עם שדות משותפים (צ'אט, דיווחים כלליים).
הפרדה שם דורשת מיזוג בצד השרת: כשתפקיד מכון כותב, `app-state-gateway` צריך לקחת את
ה-blob הקיים, להחליף רק את רשומת המכון של אותו תפקיד, ולאפשר בשדות המשותפים
**הוספה בלבד** (הוספת הודעה לצ'אט / דיווח כללי) בלי לשנות או למחוק של אחרים.
זה השלב הבא.
